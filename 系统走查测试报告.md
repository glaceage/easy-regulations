# Easy Regulations 全流程走查测试报告

> 角色视角：**经办人（owner）/ 评审人（reviewer）/ 制度管理员（admin）**
> 测试日期：2026-05-31
> 测试方式：代码审计 + 真实 ASGI 端到端接口走查（in-memory SQLite + Mock 存储）
> 自动化探针：`scripts/e2e_probe.py`（可重复运行：`SYNC_JOBS=1 python scripts/e2e_probe.py`）

---

## 一、测试方法与环境

| 项目 | 说明 |
|------|------|
| 单元/集成测试 | `SYNC_JOBS=1 pytest tests/ -q` → **56 passed**（全部通过） |
| 端到端探针 | 用真实 FastAPI 应用驱动 owner→reviewer→admin 全流程，构造 7 个账号（owner1/owner2/reviewer1/reviewer2/admin/sysadmin/reader1） |
| 探针结论 | **BUG 9 项、WARN 3 项 / 共 28 项检查** |

> 关键提示：**测试全绿 ≠ 系统没问题**。下面多数缺陷处于"测试未覆盖"的路径上，所以 56 个测试虽然全过，但真实走查仍暴露出 1 个会导致 **HTTP 500 崩溃** 的 bug、多个 **越权（IDOR）** 与 **流程闸门缺失** 问题。

---

## 二、总体结论

主干流程（draft → in_consultation → in_revision → pending_publish → published）在后端和前端都已打通，三个角色各有对应页面。但系统在三大类上"漏掉很多东西"：

1. **鉴权（最严重）**：几乎所有"读"接口和两个"创建"接口没有任何角色/归属校验，任何登录用户（含 reader）都能读取/创建他人数据；列表制度接口甚至完全公开。
2. **业务闸门不完整**：征求意见环节可在"必反馈评审人尚未发表任何意见"的情况下被关闭；评审人"是否评审完成"对经办人完全不可见。
3. **崩溃与死路**：意见"不予修改/暂缓"未填写说明时后端 **崩溃返回 500**；`target_version_label` 为空的修订会在发布环节**永久卡死且无补救入口**；`/api/jobs/{id}` 查询不存在的任务会 500。

---

## 三、缺陷清单（按严重程度）

### P0 —— 必须立即修复

#### BUG-1　意见"不予修改/暂缓"未填说明 → 服务器 500 崩溃 ✅已复现
- **现象**：经办人/管理员将某条意见置为 `rejected` 或 `deferred` 且未填 `resolution_note` 时，接口返回 **HTTP 500**（而非预期的 409 校验提示）。
- **根因**：`apps/api/services/comments.py:33-40`，函数参数名 `status: CommentStatus` 把顶部 `from fastapi import ... status` 这个模块**遮蔽**了。第 38 行 `status_code=status.HTTP_409_CONFLICT` 实际访问的是枚举对象，抛出 `AttributeError: 'CommentStatus' object has no attribute 'HTTP_409_CONFLICT'`。

```33:40:apps/api/services/comments.py
def _validate_status_resolution(status: CommentStatus, resolution_note: str | None) -> None:
    if status in {CommentStatus.REJECTED, CommentStatus.DEFERRED} and not _has_resolution_note(
        resolution_note
    ):
        raise HTTPException(
            status_code=status.HTTP_409_CONFLICT,
            detail="rejected or deferred comments require resolution_note",
        )
```

- **建议**：把参数重命名为 `comment_status`（或 `value`），保留对 `fastapi.status` 的引用；并补充对应单元测试（当前无任何测试覆盖该分支）。
- **附带影响**：前端 `RevisionWorkspacePage` 在"不予修改/暂缓"按钮上**没有前端必填校验**，导致用户极易触发该 500。

#### BUG-2　大面积越权读取（IDOR）✅已复现
所有"读"类接口仅做了 `get_current_user`（登录校验），**没有任何归属/角色校验**。探针实测：

| 接口 | 探测身份 | 实际结果 | 期望 |
|------|----------|----------|------|
| `GET /api/revisions/{id}/draft-markdown` | 未被指派的 reviewer2 | **200 读到正文** | 403 |
| `GET /api/revisions/{id}/draft-markdown` | 其他经办人 owner2 | **200 读到正文** | 403 |
| `GET /api/revisions/{id}/comments` | reader | **200 读到全部意见** | 403 |
| `GET /api/revisions/{id}/reviewers` | 任意登录用户 | 200 | 受限 |
| `GET /api/revisions/{id}/ai/suggestions` | 任意登录用户 | 200 | 受限 |
| `GET /api/revisions/{id}/files?key=` | 任意登录用户 | 前缀为 `policies/{policy_id}/`，可越权下载同一制度下任意文件 | 受限 |

- **涉及代码**：`apps/api/routers/revisions.py:41-71`、`routers/comments.py:28-35`、`routers/reviewers.py:18-25`、`routers/ai.py:37-52`。
- **建议**：新增一个 `assert_can_view_revision(revision, user)` 助手（经办人本人 / 被指派评审人 / 管理员可见），在所有读接口统一调用。

#### BUG-3　`POST /api/revisions/{id}/publish` 正常路径返回 409，发布走不通 ✅已复现
- **现象**：管理员对一个 `pending_publish` 且各闸门均已满足的修订点"发布"，返回 **409 `无法发布：目标版本号不能为空`**。
- **根因**：`RevisionCreate.target_version_label` 默认 `None`，`create_revision` 落库为 `""`（`services/revisions.py:130`）；而 `publish_revision` 要求其非空（`services/revisions.py:349-353`）。**没有任何接口可以在创建后补填版本号**，一旦为空即死路（只能 cancel）。
- **缓解现状**：前端创建修订表单把版本号设为 `required`（`PolicyDetailPage.tsx:217`），所以**正常 UI 操作可规避**；但任何走 API / 种子脚本 / 导入创建的修订都会踩坑。
- **建议**：① 后端在 `create_revision` 或进入 `pending_publish` 时校验版本号非空并给出明确提示；② 增加修订元数据更新接口（允许在 draft/in_revision 阶段补填/修改版本号）。

---

### P1 —— 影响核心流程，应尽快修

#### BUG-4　征求意见可在"必反馈评审人未发表意见"时被关闭 ✅已复现
- **现象**：owner 添加了一名 `is_mandatory=True` 的评审人（reviewer2，未发表任何意见），仍可把 `in_consultation` → `in_revision`（**HTTP 200**）。
- **根因**：必反馈闸门只在进入 `pending_publish` 时校验（`services/workflow.py:44-66`），而关闭征求意见（→`in_revision`）这一步**没有任何评审闸门**。
- **影响**：经办人可"绕过征求意见"提前进入改稿，与制度评审的业务语义不符。
- **建议**：在 `in_consultation → in_revision` 处增加必反馈校验（或做成可配置策略）。

#### BUG-5　经办人无法看到"评审进度/是否完成"
- **现象**：`feedback_status`（awaiting/submitted/closed）只暴露在评审人专属接口 `GET /api/reviews/assignments`（`services/reviewers.py:74-75` 硬性限定 reviewer 角色）。经办人**没有任何接口/页面**能看到"哪些评审人已反馈、哪些还没"。
- **现状**：经办人只能靠 `GET .../comments` 自己把 `author_id` 与评审人名单做人工比对；`ReviewerPanel` 仅展示姓名+是否必填，不展示反馈状态。
- **建议**：新增经办人可用的"评审进度"接口（聚合每位必填评审人的反馈状态），并在 ConsultationPage / 工作台展示。

#### BUG-6　`reader` 可创建制度与修订（创建类接口无角色校验）✅已复现
- **现象**：reader 角色 `POST /api/policies` 返回 **201**（成功建库）。
- **根因**：`routers/policies.py:17-25`、`routers/revisions.py:32-38` 仅 `get_current_user`，无角色限制；`services/auth.py:73-86` 定义了 `require_roles` 但**全项目从未使用**。
- **建议**：对创建/修改类接口统一接入 `require_roles`，并清理这处"已写未用"的死代码。

#### BUG-7　`GET /api/policies` / `GET /api/policies/{id}` 完全公开 ✅已复现
- **现象**：不带任何 Token 即可拉取制度列表（**200**）。
- **根因**：`routers/policies.py:28-38` 无 `Depends(get_current_user)`。
- **风险**：私有/内网部署的制度库元数据对未认证者泄露。

#### BUG-8　`GET /api/jobs/{job_id}` 越权 + 异常处理缺失 ✅已复现
- **现象一（IDOR）**：reader 可查询任意 job_id，接口无归属绑定（`routers/export.py:36-43`）。
- **现象二（崩溃/卡顿）**：查询一个非 `sync-` 开头的不存在 job 时，会尝试连 Redis；无 Redis 环境下重试 5 次后 **HTTP 500**（探针实测 500，耗时约 5 秒）。
- **建议**：将 job 与发起人/修订绑定校验；`get_job_status` 对连接失败/无效 id 返回 404 或 502 而非 500。

---

### P2 —— 一致性 / 健壮性 / 体验

#### WARN-1　`sys_admin` 不能发布，与其它管理权限不一致 ✅已复现
- `_assert_policy_admin`（`services/revisions.py:161-166`）只允许 `policy_admin`；但 `_assert_can_manage_revision`（154-158）又把 `sys_admin` 当管理员。导致 sys_admin 能改能流转，却**唯独不能发布**（探针：sys_admin 发布 403）。前端 `Layout` 还把 sys_admin 当 owner-like 展示，更易误导。

#### WARN-2　已通过闸门后仍可移除必反馈评审人，且无状态限制 ✅已复现
- `remove_reviewer`（`services/reviewers.py:218-249`）没有状态护栏：修订处于 `pending_publish` 时仍可删除必反馈评审人（探针：**204**）。可被用来"先放评审人过闸、再删除"绕过约束。

#### WARN-3　`policy_admin` 无法使用评审收件箱
- `GET /api/reviews/assignments` 硬限 reviewer 角色（探针：admin 403）。若管理员同时承担评审职责则无入口。

#### BUG-9　通知"只写不读"：无已读标记、无经办人提醒 ✅已复现
- 通知确有落库与读取（`services/notifications.py`，`GET /api/notifications` 探针返回 3 条），但：
  - `read` 字段**永远为 False**，**没有任何"标记已读"接口**（探针 POST/PATCH `/api/notifications/read` 均 404）。
  - 评审人提交意见后**不会通知经办人**；发布完成后无通知。
- 前端 `api.listNotifications()` 已实现但**没有任何页面调用**（无通知中心 UI）。

---

## 四、分角色走查结论

### 经办人（owner）
| 步骤 | 是否可用 | 备注 |
|------|----------|------|
| 创建制度 / 修订 | ✅ | 但接口层无角色校验（BUG-6） |
| 编辑草稿 / AI 起草 | ✅ | |
| 提交征求意见 | ✅ | |
| 指派评审人 | ⚠️ | 前端仅在 ConsultationPage（in_consultation）提供入口；后端其实草稿期也可指派，时机受限 |
| 查看评审进度 | ❌ | **无入口**（BUG-5） |
| 处理意见（采纳/不予/暂缓） | ⚠️ | "不予/暂缓"未填说明会 500（BUG-1），前端无必填校验 |
| 提交发布审核 | ✅ | 闸门正确拦截"有未决意见/必反馈缺失" |
| 发布 | ❌（设计如此） | 仅管理员可发布 |

### 评审人（reviewer）
| 步骤 | 是否可用 | 备注 |
|------|----------|------|
| 登录后进入"我的评审" | ✅ | 首页重定向到 `/reviews` |
| 找到待评审任务 | ✅ | `in_consultation` 行可"开始评审" |
| 按章节提意见 | ✅ | 仅被指派且处于征求意见阶段可提交（后端校验正确） |
| 明确"完成评审"动作 | ❌ | 无"标记完成"按钮，完成状态靠"是否有意见"隐式推断 |
| 未被指派时的体验 | ⚠️ | ConsultationPage 仍渲染评论表单，提交才被后端 403 拒绝（前端泄露 UI） |
| 征求意见关闭后 | ⚠️ | 无法再评论，且无 `in_revision` 阶段的 UI 入口 |

### 制度管理员（admin）
| 步骤 | 是否可用 | 备注 |
|------|----------|------|
| 发现"待发布"任务 | ⚠️ | 无专属待办队列，需自行从制度详情里翻找 `pending_publish` |
| 预览正文/意见/PDF | ✅ | |
| 确认发布 | ⚠️ | 正常 UI 可发布；但 `target_version_label` 为空时 409 死路（BUG-3）。前端发布前只校验"未决意见数"，不校验"必反馈/解决说明"，真正失败仅由后端 409 兜底，缺少友好提示 |

---

## 五、前端专项问题

| 编号 | 问题 | 位置 |
|------|------|------|
| FE-1 | **路由无角色保护**：`ProtectedRoute` 只校验是否登录，任何角色都能用 URL 直达 `/admin/policies/new`、`/reviews` 等（非授权页面靠"页面内 UI 隐藏"，非真正防护） | `components/ProtectedRoute.tsx:4-8` |
| FE-2 | "不予修改/暂缓"无前端必填校验，直接触发 BUG-1 的 500 | `RevisionWorkspacePage.tsx:159-176` |
| FE-3 | 经办人在发布页看不到"必反馈未完成/缺解决说明"的具体原因（前端只看 openCount） | `PublishPage.tsx:167,251` |
| FE-4 | 通知 API、导入 docx API 已在 `client.ts` 实现但**无任何 UI 调用** | `client.ts:222-228,252-254` |
| FE-5 | `components/Breadcrumb.tsx` 定义后从未被引用（死代码） | `components/Breadcrumb.tsx` |
| FE-6 | ConsultationPage 返回链接对所有角色都写死"← 我的评审"，对经办人/管理员是错误返回目标 | `ConsultationPage.tsx:165` |
| FE-7 | PoliciesPage 空状态对所有人（含 reviewer）显示"点击新建" | `PoliciesPage.tsx:55-58` |
| FE-8 | 管理员无"待发布列表"导航；reader 角色无专属 UX | `Layout.tsx` |

---

## 六、建议修复优先级

1. **BUG-1**（500 崩溃）：改参数名 + 补测试 —— 1 行改动，立即修。
2. **BUG-2 / FE-1**（越权读取 + 路由无防护）：新增统一的可见性校验助手并接入所有读接口；前端补角色路由守卫。
3. **BUG-3**（发布死路）：创建/流转时校验版本号非空 + 增加补填入口。
4. **BUG-4 / BUG-5**（征求意见闸门 + 评审进度可见性）：补必反馈闸门 + 经办人评审进度接口与展示。
5. **BUG-6 / BUG-7 / WARN-1**（创建鉴权 / 公开接口 / sys_admin 发布一致性）：统一接入 `require_roles`，理顺角色矩阵。
6. **BUG-8 / WARN-2**（job 越权与 500 / 评审人可被违规移除）：补归属校验与状态护栏。
7. **BUG-9 / FE-4**（通知闭环）：补"标记已读"接口、提交意见时提醒经办人、前端通知中心。
8. **P2 / FE 体验项**：清理死代码、修正返回链接、管理员待办队列等。

---

## 七、复现方式

```bash
# 1. 单元/集成测试（全绿，但覆盖不到上述缺陷）
SYNC_JOBS=1 pytest tests/ -q

# 2. 多角色端到端探针（输出 BUG/WARN 汇总）
SYNC_JOBS=1 python scripts/e2e_probe.py
```

探针实测汇总（2026-05-31）：**BUG 9 / WARN 3 / 共 28 项检查**，与本报告逐条对应。

---

> 备注：本次仅做"测试与报告"，未修改任何业务代码。新增的 `scripts/e2e_probe.py` 为可重复运行的走查探针，供后续修复时做回归验证；如不需要可删除。
